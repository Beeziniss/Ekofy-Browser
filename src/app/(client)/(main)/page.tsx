// Root route is now handled by middleware
// Users are redirected to /landing (unauthenticated) or appropriate dashboard (authenticated)
// The previous home content has been moved to /home route

const RootPage = () => {
  return null;
};

export default RootPage;
